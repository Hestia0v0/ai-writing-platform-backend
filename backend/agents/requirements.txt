fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
pydantic==2.7.1
httpx==0.27.0

# OpenAI-compatible SDK — used for DeepSeek and any OpenAI-compatible provider
openai>=1.30.0

# US-17 caching — Redis async client (optional; falls back to in-memory LRU when not installed)
redis>=5.0.0
